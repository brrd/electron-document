{
  "name": "electron-document",
  "version": "0.1.0",
  "description": "Manage documents in Electron (open, save, close...)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "brrd",
  "license": "MIT",
  "devDependencies": {
    "proxyquire": "^1.8.0"
  }
}
